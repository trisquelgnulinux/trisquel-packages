# Note: to enable sending the hostname to your local router during DHCP requests, see /etc/dhcp/dhclient.conf

# Uncomment the next 4 lines to enable random (but permanent per-network) mac addresses. Then run 'sudo service network-manager restart'
#[connection]
#wifi.cloned-mac-address=stable
#ethernet.cloned-mac-address=stable
#connection.stable-id=\${CONNECTION}/\$(BOOT)
